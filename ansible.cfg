[defaults]
# This repo IS the `bootstrap-online-machine` role (role dirs live at the repo
# root). Adding the parent directory to roles_path lets configure_online_laptop.yml
# resolve the role by name. Requires the repo be cloned as `bootstrap-online-machine`.
roles_path = ../
